-------------------------------------------------------------------
     
                        openMDX/CSS

-------------------------------------------------------------------

The Java archive openmdx-css.jar contais
* the css1 constant generator package
* the css1 constant packages for the openMDX models
  openMDX/Core
  openMDX/Security
  openMDX/Portal
* the JavaDoc for the openMDX sub-projects
  openMDX/Core
  openMDX/Security
  openMDX/Portal
  openMDX/CSS

The corresponding model build format value is
--format=ch.css.model1.mapping.java.Constants_1

-------------------------------------------------------------------
